// Just practing how to use the reduce method in JS.

array = [2, 3, 7, 10];

const somaReduce = array.reduce((valorAcumulado, numeroDoArray) => valorAcumulado * numeroDoArray, 1)

console.log(somaReduce)